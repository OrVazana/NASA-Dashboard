<Window x:Class="NASA.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NASA" xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Icon="Images/icon.ico"
        Title="Nasa" Height="600" Width="960">
 

    <Grid x:Name="mainGrid" Background="#FF333433">
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Background="#FF851099" Height="30" MouseDown="Grid_MouseDown">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,4,0">
                <Button  x:Name="ButtunMinimize" ToolTip="Minimize" Width="40" Height="30" Background="{x:Null}" BorderBrush="{x:Null}" Padding="0" Click="ButtonMinimize_Click">
                    <materialDesign:PackIcon Kind="WindowMinimize"/>
                </Button>
                <Button x:Name="ButtonMaximize" ToolTip="Maximize" Width="40" Height="30" Background="{x:Null}" BorderBrush="{x:Null}" Padding="0"  Click="ButtonMaximaize_Click">
                    <materialDesign:PackIcon Kind="WindowMaximize"/>
                </Button>
                <Button x:Name="ButtonClose" ToolTip="Close" Width="40" Height="30" Background="{x:Null}" BorderBrush="{x:Null}" Padding="0"  Click="ButtonClose_Click" Style="{StaticResource MaterialDesignFlatLightButton}">
                    <materialDesign:PackIcon Kind="WindowClose"/>
                </Button>
            </StackPanel>
        </Grid>
        <StackPanel Margin="0,10,0,0" HorizontalAlignment="Left" Orientation="Horizontal" VerticalAlignment="Center" Grid.Row="1">
            <Rectangle Width="8" Fill="Transparent"></Rectangle>
            <Image Height="60" Width="80" Source="/NASA;component/Images/nasa-logo.png"/>
            <TextBlock Text="NASA" FontSize="40" Foreground="White" FontWeight="DemiBold" VerticalAlignment="Center" />
        </StackPanel>
        <StackPanel Margin="0,10,0,0" HorizontalAlignment="Center" Orientation="Horizontal" VerticalAlignment="Center" Background="Transparent" Grid.Row="1">
            <!-- 0 Home button-->
            <Button Command="{Binding DisplayHomeView}" BorderBrush="{x:Null}" Background="{x:Null}" ToolTip="Home"  Style="{StaticResource MaterialDesignFlatLightButton}">
                <materialDesign:PackIcon Kind="Home" Width="30" Height="30" VerticalAlignment="Center" HorizontalAlignment="Center"/>
            </Button>
            <!-- 1 photo of the day button-->
            <Button Command="{Binding DisplayTodayPhotoView}" BorderBrush="{x:Null}" Background="{x:Null}" ToolTip="Astronomy Picture of the Day"   Style="{StaticResource MaterialDesignFlatLightButton}">
                <materialDesign:PackIcon Kind="ImageFrame" Width="30" Height="30" VerticalAlignment="Center" HorizontalAlignment="Center"/>
            </Button>
            <!-- 2 solar system data button-->
            <Button Command="{Binding DisplaySolarSystemView}" BorderBrush="{x:Null}" Background="{x:Null}" ToolTip="Solar system data"   Style="{StaticResource MaterialDesignFlatLightButton}">
                <materialDesign:PackIcon Kind="Planet" Width="30" Height="30" VerticalAlignment="Center" HorizontalAlignment="Center"/>
            </Button>
            <!-- 3 NASA Image Library button-->
            <Button Command="{Binding DisplayLibraryView}" BorderBrush="{x:Null}" Background="{x:Null}" ToolTip="NASA Image Library"    Style="{StaticResource MaterialDesignFlatLightButton}">
                <materialDesign:PackIcon Kind="PhotoLibrary" Width="30" Height="30" VerticalAlignment="Center" HorizontalAlignment="Center"/>
            </Button>
            <!-- 4 Asteroids status button-->
            <Button Command="{Binding DisplayAstroidsView}" BorderBrush="{x:Null}" Background="{x:Null}" ToolTip="Asteroids status"   Style="{StaticResource MaterialDesignFlatLightButton}">
                <materialDesign:PackIcon Kind="SqlQuery" Width="30" Height="30" VerticalAlignment="Center" HorizontalAlignment="Center"/>
            </Button>
        </StackPanel>
        <StackPanel Margin="0,10,20,0" HorizontalAlignment="Right" Orientation="Horizontal" VerticalAlignment="Center" Background="Transparent" Grid.Row="1">
            <!-- 5 contact us button-->
            <Button Command="{Binding DisplayContactUsView}" BorderBrush="{x:Null}" Background="{x:Null}" ToolTip="Contact us"   Style="{StaticResource MaterialDesignFlatLightButton}">
                <materialDesign:PackIcon Kind="Github" Width="30" Height="30" VerticalAlignment="Center" HorizontalAlignment="Center"/>
            </Button>
            <!-- 6 About us button-->
            <Button Command="{Binding DisplayAboutUsView}" BorderBrush="{x:Null}" Background="{x:Null}" ToolTip="About us"   Style="{StaticResource MaterialDesignFlatLightButton}">
                <materialDesign:PackIcon Kind="AboutCircleOutline" Width="30" Height="30" VerticalAlignment="Center" HorizontalAlignment="Center"/>
            </Button>
            <Rectangle Width="3" Fill="Transparent"></Rectangle>
        </StackPanel>

        <!-- current user control -->

        <materialDesign:Card Margin="10" Grid.Row="2" UniformCornerRadius="20">
            <Grid>
                <ContentControl Content="{Binding Path=Currents.CurrentVM, Source={x:Static Application.Current}}"></ContentControl>
            </Grid>
        </materialDesign:Card>
    </Grid>
</Window>
