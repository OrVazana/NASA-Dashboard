<Application x:Class="NASA.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:NASA" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:views="clr-namespace:NASA.Views"
             xmlns:viewmodels="clr-namespace:NASA.ViewsModels"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <LinearGradientBrush x:Key="StandardBorderBrush" EndPoint="0.0,0.0" StartPoint="1,1">
                <GradientStop Color="#FF81A378" Offset="0.0"/>
                <GradientStop Color="#00FFFFFF" Offset="0.5"/>
                <GradientStop Color="#FF81A378" Offset="1.0"/>
            </LinearGradientBrush>
            <DataTemplate DataType="{x:Type viewmodels:HomeVM}">
                <views:HomeView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type viewmodels:SolarSystemVM}">
                <views:SolarSystemView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type viewmodels:TodayPhotoVM}">
                <views:TodayPhotoView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type viewmodels:AboutUsVM}">
                <views:AboutUsView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type viewmodels:ContactUsVM}">
                <views:ContactUsView/>
            </DataTemplate>

            <ResourceDictionary.MergedDictionaries>
                <materialDesign:BundledTheme BaseTheme="Dark" PrimaryColor="Purple" SecondaryColor="LightBlue" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Defaults.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Application.Resources>
</Application>
